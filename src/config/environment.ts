const HOST_URL = process.env.REACT_APP_HOST_URL;
const HOST_API = process.env.REACT_APP_HOST_API;

const JWT_PREFIX = `${process.env.REACT_APP_JWT_PREFIX}`;

export { HOST_URL, HOST_API, JWT_PREFIX };
