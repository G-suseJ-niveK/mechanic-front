// routes
// import { PATH_DASHBOARD } from '../../routes/paths';

const sidebarConfig = [
  {
    subheader: 'Visualización',
    items: [
      {
        title: 'Análisis',
        path: '/dashboard',
        icon: 'timeline'
      }
    ]
  }
];

export default sidebarConfig;
